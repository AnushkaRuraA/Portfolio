Drop your resume PDF in this /public folder named exactly:

    resume.pdf

The "Download Resume" button on the site links to /resume.pdf.
(You can change the link from the admin panel: /mod → Profile → Resume URL.)
